#include <iostream>
using namespace std;
int main(){
    int  a;
    long long b;
    char c;
    float d;
    double e; //use "fixed" keyword in cout for exact output of double
    cin >> a >> b >> c >> d >> e;
    cout << a << '\n' << b << '\n' << c << '\n' << d << '\n' << e << endl;
}